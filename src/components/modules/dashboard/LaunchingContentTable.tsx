import styled from "styled-components";

const LaunchingContentTable = () => {
    return (
        <StyledTable>이달의 런칭 작품 분석 표</StyledTable>
    )
}

const StyledTable = styled.div`
    width: 800px;
    height: 350px;
    border: 1px solid black;
`

export default LaunchingContentTable;